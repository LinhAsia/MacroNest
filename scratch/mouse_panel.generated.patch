diff --git "a/src/ui/mouse_panel.rsD:\\app\\MacroNest\\scratch\\mouse_panel.head.rs" "b/src/ui/mouse_panel.rsD:\\app\\MacroNest\\scratch\\mouse_panel.patched.rs"
index 103e123..1f762ca 100644
--- "a/src/ui/mouse_panel.rsD:\\app\\MacroNest\\scratch\\mouse_panel.head.rs"
+++ "b/src/ui/mouse_panel.rsD:\\app\\MacroNest\\scratch\\mouse_panel.patched.rs"
@@ -2828,7 +2828,7 @@ impl CrosshairApp {
                 }
                 file.write_all(&buffer[..n])?;
                 downloaded += n as u64;
-                let p = (downloaded as f32 / total_size as f32 * 1000.0) as u32;
+                let p = ((downloaded as f32 / total_size as f32) * 999.0).round() as u32;
                 progress.store(p, std::sync::atomic::Ordering::SeqCst);
             }
 
@@ -2853,7 +2853,7 @@ impl CrosshairApp {
                 anyhow::bail!("Failed to extract arduino_tools.zip");
             }
 
-            let _ = std::fs::remove_file(&paths.arduino_tools_zip);
+            let _ = std::fs::remove_file(&paths.arduino_tools_zip);`r`n            progress.store(1000, std::sync::atomic::Ordering::SeqCst);
 
             Ok(())
         });
@@ -3332,3 +3332,4 @@ fn patch_hex_descriptors(hex_content: &str, vid: u16, pid: u16) -> anyhow::Resul
 
     Ok(patched_lines.join("\r\n"))
 }
+
